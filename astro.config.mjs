// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Địa chỉ web thật — CẦN có thì sitemap.xml + ảnh chia sẻ (og:image) + Schema mới chạy đúng.
  site: 'https://chuxetai.com',
});
